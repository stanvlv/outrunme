import { Box, NativeBaseProvider } from "native-base";

export default function ForgotPassword({ navigation }) {
    return (
        <NativeBaseProvider>
        <Box>Well I don't care. Make a new account</Box>
        </NativeBaseProvider>
    )
}