import { NativeBaseProvider, Box } from "native-base"

export default function RunMap({ navigation }) {
    return (
        <>
        <NativeBaseProvider>
        <Box>Hello from Map Screen</Box>
        </NativeBaseProvider>

        </>
    )
}